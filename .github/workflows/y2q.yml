name: Build for y2q

on:
  push:
    branches: [ "sm8250" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-y2q:
    uses: ./.github/workflows/build_sm8250.yml
    with:
      device: y2q
      device_name: Galaxy S20 Plus 5G
